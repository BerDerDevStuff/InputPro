-- InputPro Bootstrap (auto-generated by InputPro Plugin)
-- This script is what the plugin creates in StarterPlayerScripts when you export.
-- You don't need to write this yourself — it's here as a reference.
--
-- Place in: StarterPlayerScripts/InputProBootstrap
-- Set the "ConfigName" attribute to your exported config name.

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local InputProService = require(
	ReplicatedStorage
	:WaitForChild("InputProRuntime")
	:WaitForChild("InputProService")
)

local configName = script:GetAttribute("ConfigName")
if configName then
	InputProService.Init(configName)
end
